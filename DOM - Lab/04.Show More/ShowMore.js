function showText() {
    let anchor = document.getElementById('more');
    let span = document.getElementById('text');

    // When clicked i need to remove link and put text on the same place
    anchor.style.display='none';
    span.style.display='inline'
}